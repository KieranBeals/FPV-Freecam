# Agent Instructions

- Be brief in responses.
- When testing, use Nix: run `nix run .#compile` to make sure the project compiles.
- Once a feature is done, double check by running `./launch-all-clients.sh` for 60 seconds and make sure all clients launch without errors.
- It is ok if one client exited, as long as it reached the main menu first. That means I exited it.
